[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
# Increase build timeout for Swift compilation (in seconds)
buildCommand = ""

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
